# -*- coding: utf-8 -*-

import json
import requests

endpoint = "https://nge398cff5.execute-api.us-east-1.amazonaws.com/api"

# res = requests.post(f"{endpoint}/users", data=json.dumps({"username": "alice"}), headers={"Content-Type": "application/json"})
# print(res.text)

# res = requests.get(f"{endpoint}/users/alice")
# print(res.text)